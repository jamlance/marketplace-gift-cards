# marketplace-gift-cards

Deploy repo for the **gift-cards** Inkress-embedded app. Generated from `jamlance/bookerva-apps` — edit there, then re-run `scripts/sync-app-repo.mjs gift-cards --push`.

Coolify: base_directory `/`, dockerfile `/apps/gift-cards/Dockerfile`.
